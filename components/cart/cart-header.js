import classes from "./cart-header.module.css"

const CartHeader = () => {
    return (
        <>
            <h1 className={classes.header}>
                Your Products
            </h1>
        </>
    )
}

export default CartHeader